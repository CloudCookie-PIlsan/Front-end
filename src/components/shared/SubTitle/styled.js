import styled from "styled-components";

export const StSubTitle = styled.h2`
    font-size: 1.2rem;
    line-height: 1.5;
    font-weight: 600;
    word-spacing: -0.05rem;

    // 날짜 표기
    & > span {
        font-size: 0.8rem;
        font-weight: 600;
        margin-left: 0.5rem;
    }
`;
